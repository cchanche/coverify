{
  "name": "coverify",
  "packageManager": "yarn@3.3.1",
  "license": "MIT",
  "private": true,
  "engines": {
    "node": "18.4.0"
  },
  "workspaces": [
    "packages/**"
  ],
  "scripts": {
    "start": "docker-compose up",
    "reinstall": "rm -rf node_modules && yarn workspaces run remove-modules && yarn install"
  }
}
